#!/bin/sh

#
# transadd - add a torrent
#

pidof transmission-daemon >/dev/null || (transmission-daemon && noti " Torrent" "Daemon started" && sleep 3)

transmission-remote -a "$@" && noti " Torrent" "File added"
