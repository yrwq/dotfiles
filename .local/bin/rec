#!/bin/sh

ffmpeg -f x11grab\
    -framerate 60 \
    -thread_queue_size 512 \
    -i "$DISPLAY".0+"$X","$Y"\
    -vcodec libx264\
    -qp 18\
    -preset ultrafast \
    "$HOME/etc/vid/rec/$(date +'%a_%I:%M').mp4"
