#!/bin/sh

#
# mail - sync mails and open mail client
#

notify-send " Syncing e-mails"

mw -Y

unread="$(find "${XDG_DATA_HOME:-$HOME/.local/share}"/mail/*/[Ii][Nn][Bb][Oo][Xx]/new/* -type f | wc -l 2>/dev/null)"

notify-send " E-mails synced" "Unread: $unread"
st -c mail -e neomutt
