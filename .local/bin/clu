#!/usr/bin/env bash

#
# clu - command line utilities
#

# Search for a string in a directory
dg(){
    grep --color=auto -ri "$1" *
    # grep -rni "${1}" *    # include line numbers
}

case "$1" in
    dg)
        dg "$2"
    ;;
esac
