#!/usr/bin/env bash

# case $1 in
# 	*.mkv|*.mp4|*.png|*.jpg|*.jpeg)
# 		curl -F'file=@${1}' https://0x0.st

type=
file -ibL "$1" | grep -q '^text/' && type=';type=text/plain'

curl -sF file="@${1:--}$type" 'http://0x0.st' \
	| tee /dev/stderr \
	| xclip -r -sel clip
